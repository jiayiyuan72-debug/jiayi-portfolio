services:
  jiayi-portfolio:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: jiayi-portfolio
    restart: unless-stopped
    env_file:
      - .env.production
    ports:
      - "3000:3000"
